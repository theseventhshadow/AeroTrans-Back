spring.application.name=ms-aerotrans-transfers
server.port=${SERVER_PORT:8081}

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/aerotrans}
spring.datasource.username=${DB_USERNAME:aerotrans}
spring.datasource.password=${DB_PASSWORD:aerotrans}
spring.jpa.hibernate.ddl-auto=${DDL_AUTO:update}
spring.jpa.open-in-view=false
